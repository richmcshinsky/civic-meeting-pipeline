# Runtime dependencies for the deployed Streamlit demo only.
# Streamlit Community Cloud installs from this file. The full pipeline deps
# live in pyproject.toml; the demo only reads precomputed files, so it needs
# just these three.
streamlit>=1.33
pandas>=2.2
pyarrow>=15.0
plotly>=5.20
